require 'rails_helper'

RSpec.describe WritingTip, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:writing_tip)
  #   expect(record).to be_persisted
  # end
end
