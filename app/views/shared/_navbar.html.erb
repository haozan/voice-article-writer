<nav class="bg-transparent backdrop-blur-sm border-b border-gray-200/50 dark:border-gray-700/50">
  <div class="container-lg mx-auto px-4 py-3">
    <div class="flex items-center justify-between">
      <div class="flex items-center gap-3">
        <%= link_to root_path, class: "flex items-center gap-3" do %>
          <%= image_tag "logo.png", alt: Rails.application.config.x.appname, class: "w-10 h-10" %>
          <span class="text-xl font-bold text-gray-900 dark:text-gray-50"><%= Rails.application.config.x.appname %></span>
        <% end %>
      </div>
    </div>
  </div>
</nav>
