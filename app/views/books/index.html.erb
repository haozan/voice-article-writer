<% content_for :title, "我的书架" %>

<div class="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 dark:from-gray-900 dark:to-gray-800 py-12">
  <div class="container-xl mx-auto px-4">
    
    <!-- Page Header -->
    <div class="text-center mb-12">
      <h1 class="text-5xl font-bold text-gray-900 dark:text-gray-50 mb-4">
        📚 我的书架
      </h1>
      <p class="text-lg text-gray-600 dark:text-gray-400">
        精选书籍收藏，记录阅读旅程
      </p>
    </div>

    <!-- Search and Filter Bar -->
    <div class="card bg-white dark:bg-gray-800 shadow-lg mb-8">
      <div class="card-body p-6">
        <%= form_with url: books_path, method: :get, local: true, class: "flex flex-col md:flex-row gap-4" do |f| %>
          <!-- Search Input -->
          <div class="flex-1">
            <%= f.text_field :search, 
                  value: params[:search],
                  placeholder: "搜索书名或作者...", 
                  class: "form-input w-full" %>
          </div>

          <!-- Sort Selector -->
          <div class="w-full md:w-48">
            <%= f.select :sort, 
                  options_for_select([
                    ['最新创建', 'newest'],
                    ['最近更新', 'updated'],
                    ['书名排序', 'title']
                  ], params[:sort]),
                  {},
                  class: "form-select w-full" %>
          </div>

          <!-- Submit Button -->
          <div>
            <%= f.submit "筛选", class: "btn-primary w-full md:w-auto" %>
          </div>
          
          <!-- Reset -->
          <% if params[:search].present? || params[:sort].present? %>
            <%= link_to "重置", books_path, class: "btn-ghost w-full md:w-auto" %>
          <% end %>
        <% end %>
      </div>
    </div>

    <!-- Books Grid -->
    <% if @books.any? %>
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-6 mb-12">
        <% @books.each do |book| %>
          <%= link_to book_path(book), class: "group block" do %>
            <div class="relative transition-all duration-300 transform hover:scale-105 hover:shadow-2xl">
              <!-- Pinned Badge -->
              <% if book.pinned %>
                <div class="absolute top-2 right-2 z-10">
                  <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-bold bg-yellow-400 text-yellow-900 shadow-lg">
                    <%= lucide_icon "pin", class: "w-3 h-3 mr-1" %>
                    置顶
                  </span>
                </div>
              <% end %>

              <!-- Book Cover -->
              <div class="relative overflow-hidden rounded-lg shadow-lg aspect-[10/14] bg-white dark:bg-gray-800">
                <%= book_cover_svg(book).html_safe %>
              </div>

              <!-- Book Info -->
              <div class="mt-4">
                <h3 class="font-bold text-gray-900 dark:text-gray-50 text-sm line-clamp-2 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                  <%= book.title %>
                </h3>
                <% if book.author.present? %>
                  <p class="text-xs text-gray-600 dark:text-gray-400 mt-1">
                    <%= book.author %>
                  </p>
                <% end %>
                <div class="flex items-center mt-2 text-xs text-gray-500 dark:text-gray-500">
                  <%= lucide_icon "book-open", class: "w-3 h-3 mr-1" %>
                  <span><%= book.chapters.count %> 章节</span>
                  <span class="mx-2">·</span>
                  <%= lucide_icon "file-text", class: "w-3 h-3 mr-1" %>
                  <span><%= book.articles_count %> 文章</span>
                </div>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>

      <!-- Pagination -->
      <div class="flex justify-center">
        <%= paginate @books %>
      </div>
    <% else %>
      <!-- Empty State -->
      <div class="text-center py-24">
        <div class="w-32 h-32 mx-auto mb-6 bg-gray-100 dark:bg-gray-700 rounded-full flex items-center justify-center">
          <%= lucide_icon "book", class: "w-16 h-16 text-gray-400" %>
        </div>
        <h3 class="text-2xl font-bold text-gray-900 dark:text-gray-50 mb-2">
          还没有书籍
        </h3>
        <p class="text-gray-600 dark:text-gray-400 mb-8">
          <%= if params[:search].present?
                "没有找到匹配的书籍，试试其他关键词？"
              else
                "开始归档你的文章，创建第一本书吧！"
              end %>
        </p>
        <% if params[:search].present? %>
          <%= link_to "查看全部书籍", books_path, class: "btn-primary" %>
        <% end %>
      </div>
    <% end %>

  </div>
</div>
